name: "Build"

on:
  workflow_dispatch:
    inputs:
      release:
        description: "Create a release (tag, publish, draft github release, then bump)"
        required: true
        default: false
        type: boolean
  push:
    branches: [ "**" ]
    tags-ignore: [ "**" ]
  pull_request:

concurrency:
  group: "${{ github.workflow }}-${{ github.event.number || github.ref }}"
  cancel-in-progress: true

env:
  ORG_GRADLE_PROJECT_is_release: ${{ github.event_name == 'workflow_dispatch' && inputs.release }}

jobs:
  action:
    name: "Action"
    # Only run on PRs if the source branch is on someone else's repo
    if: ${{ github.event_name != 'pull_request' || github.repository != github.event.pull_request.head.repo.full_name }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:

      - name: "Checkout Repository"
        uses: actions/checkout@v4
        with:
          fetch-tags: true
          fetch-depth: 0

      - name: "Setup Java 25"
        uses: actions/setup-java@v4
        with:
          java-version: "25"
          distribution: "temurin"

      - name: "Setup Gradle"
        uses: gradle/actions/setup-gradle@v4

      - name: "Get And Validate Version"
        id: version
        run: |
          version="$(./gradlew properties -q | grep '^version:' | cut -d ':' -f 2 | xargs)"
          .github/scripts/version-validate.sh "$version" "$ORG_GRADLE_PROJECT_is_release"
          echo "version=${version%-SNAPSHOT}" >> "$GITHUB_OUTPUT"

      - name: "Build Gradle"
        run: ./gradlew build

      - name: "Upload Jar"
        uses: actions/upload-artifact@v4
        with:
          name: artifacts
          path: "build/libs/foundation.jar"
          if-no-files-found: error

      - name: "Upload Test Results"
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            build/test-results/test/TEST-*.xml
            build/reports/

      - name: "Publish Test Results"
        if: ${{ always() }}
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          check_name: "test results"
          files: "build/test-results/test/TEST-*.xml"

      - name: "Create Release Tag"
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.release }}
        run: |
          git tag "v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

      - name: "Publish to Maven"
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.release) || github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/heads/dev') }}
        run: ./gradlew publish
        env:
          ORG_GRADLE_PROJECT_xpdustryUsername: "${{ secrets.XPDUSTRY_MAVEN_USERNAME }}"
          ORG_GRADLE_PROJECT_xpdustryPassword: "${{ secrets.XPDUSTRY_MAVEN_PASSWORD }}"
          ORG_GRADLE_PROJECT_signingKey: "${{ secrets.XPDUSTRY_MAVEN_SIGNING_KEY }}"
          ORG_GRADLE_PROJECT_signingPassword: "${{ secrets.XPDUSTRY_MAVEN_SIGNING_PASSWORD }}"

      - name: "Generate Changelog"
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.release }}
        run: .github/scripts/changelog-generate.sh "$GITHUB_REPOSITORY" ".GENERATED_CHANGELOG.md"

      - name: "Create Draft Release"
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.release }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ steps.version.outputs.version }}"
          name: "v${{ steps.version.outputs.version }}"
          draft: true
          body_path: ".GENERATED_CHANGELOG.md"
          files: "build/libs/foundation.jar"
          fail_on_unmatched_files: true

      - name: "Bump Version"
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.release }}
        run: .github/scripts/version-bump.sh "VERSION.txt" "${{ steps.version.outputs.version }}"

      - name: "Commit Version Bump"
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.release }}
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "chore(release): Post v${{ steps.version.outputs.version }} changes"
          file_pattern: "VERSION.txt"
