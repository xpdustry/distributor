package fr.xpdustry.distributor.plugin.commands;


import mindustry.gen.*;

import fr.xpdustry.distributor.command.context.*;
import fr.xpdustry.distributor.command.mindustry.*;
import fr.xpdustry.distributor.command.param.string.*;

import io.leangen.geantyref.*;
import org.jetbrains.annotations.*;

import java.util.*;


/*
public class JavaScriptCommand extends CommandContainer<Playerc>{
    public JavaScriptCommand(ContextRunner<Playerc> responseHandler){
        super("jscript", "Some JavaScript utilities.", Arrays.asList(
            new StringParameter("cmd").withDefaultValue("help").withOptional(true),
            new StringParameter("args").withDefaultValue("").withOptional(true)
        ), responseHandler, TypeToken.get(Playerc.class));
    }

    @Override
    protected void execute(@NotNull CommandContext<Playerc> context) throws Exception{

    }
}

 */
